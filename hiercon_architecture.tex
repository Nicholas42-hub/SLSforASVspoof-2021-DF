\documentclass[border=8pt, multi, tikz]{standalone} 
\usepackage{import}
\subimport{PlotNeuralNet/layers/}{init}
\usetikzlibrary{positioning}
\usetikzlibrary{3d} %for including external image 

\def\ConvColor{rgb:yellow,5;red,2.5;white,5}
\def\ConvReluColor{rgb:yellow,5;red,5;white,5}
\def\PoolColor{rgb:red,1;black,0.3}
\def\UnpoolColor{rgb:blue,2;green,1;black,0.3}
\def\FcColor{rgb:blue,5;red,2.5;white,5}
\def\FcReluColor{rgb:blue,5;red,5;white,4}
\def\SoftmaxColor{rgb:magenta,5;black,7}   
\def\SumColor{rgb:blue,5;green,15}

\newcommand{\copymidarrow}{\tikz \draw[-Stealth,line width=0.8mm,draw={rgb:blue,4;red,1;green,1;black,3}] (-0.3,0) -- ++(0.3,0);}

\begin{document}
\begin{tikzpicture}
\tikzstyle{connection}=[ultra thick,every node/.style={sloped,allow upside down},draw=\edgecolor,opacity=0.7]
\tikzstyle{copyconnection}=[ultra thick,every node/.style={sloped,allow upside down},draw={rgb:blue,4;red,1;green,1;black,3},opacity=0.7]

% Input placeholder
\node[canvas is zy plane at x=0] (input) at (-3,0,0) {\includegraphics[width=6cm,height=1.5cm]{PlotNeuralNet/examples/fcn8s/cats.jpg}};

% XLS-R Encoder
\pic[shift={ (2,0,0) }] at (input-east) 
    {RightBandedBox={
        name=xlsr,
        caption=XLS-R\\24 Layers,
        xlabel={{ 128, 128 }},
        zlabel=1024,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=32,
        width={ 2.5 , 2.5 },
        depth=32
        }
    };

\draw [connection]  (input-east)    -- node {\midarrow} (xlsr-west);

% Stage 1: Temporal Attention
\pic[shift={ (2,0,0) }] at (xlsr-east) 
    {Box={
        name=stage1,
        caption=Stage 1\\Temporal,
        fill=\PoolColor,
        opacity=0.6,
        height=28,
        width=2,
        depth=28
        }
    };

\draw [connection]  (xlsr-east)    -- node {\midarrow} (stage1-west);

% Stage 2: Intra-Group Attention
\pic[shift={ (2,0,0) }] at (stage1-east) 
    {Box={
        name=stage2,
        caption=Stage 2\\Intra-Group,
        fill=\PoolColor,
        opacity=0.6,
        height=22,
        width=2,
        depth=22
        }
    };

\draw [connection]  (stage1-east)    -- node {\midarrow} (stage2-west);

% Stage 3: Inter-Group Attention
\pic[shift={ (2,0,0) }] at (stage2-east) 
    {Box={
        name=stage3,
        caption=Stage 3\\Inter-Group,
        fill=\PoolColor,
        opacity=0.6,
        height=14,
        width=2,
        depth=14
        }
    };

\draw [connection]  (stage2-east)    -- node {\midarrow} (stage3-west);

% Embedding Layer
\pic[shift={(2,0,0)}] at (stage3-east) 
    {Box={
        name=embedding,
        caption=Embedding,
        xlabel={{" ","dummy"}},
        zlabel=10,
        fill=\SoftmaxColor,
        opacity=0.8,
        height=10,
        width=1.5,
        depth=10
        }
    };

\draw [connection]  (stage3-east)    -- node {\midarrow} (embedding-west);

% Classifier Branch
\pic[shift={(2,3,0)}] at (embedding-east) 
    {Box={
        name=classifier,
        caption=Classifier,
        zlabel=2,
        fill=\SoftmaxColor,
        height=8,
        width=1.5,
        depth=8
        }
    };

\draw [connection]  (embedding-east)    -- node {\midarrow} (classifier-west);

% Contrastive Projection Branch
\pic[shift={(2,-3,0)}] at (embedding-east) 
    {Box={
        name=contrastive,
        caption=Projection,
        zlabel=256,
        fill=\SoftmaxColor,
        height=8,
        width=1.5,
        depth=8
        }
    };

\draw [connection]  (embedding-east)    -- node {\midarrow} (contrastive-west);

\end{tikzpicture}
\end{document}
